!!!
%html
  %head
    %title Interview
    = stylesheet_link_tag "application", :media => "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    %header
      .dropdown-header
        %ul.nav.nav-pills
          %li
            = edit_link Student,"Home",students_path
            %li
              -#= link_to "Add Topic",new_topic_path
              = create_link Topic, "Add Topic", new_topic_path
            %li
              -#= link_to "Add Question",new_question_path
              =create_link Question, "Add Question",new_question_path
            %li
              -#= link_to "View Answer",student_answer_questions_path
              = non_restfull_link Question,:destroy,"View Answer",student_answer_questions_path
              %li.divider
        %ul
          - if user_signed_in?
            #{link_to "Sign out", destroy_user_session_path, :method => :delete}
          -else
            = link_to "Sign up", new_user_registration_path
            or #{link_to "sign in", new_user_session_path}
      .cls
    .container
      - flash.each do |key, value|
        %div{:class=>"alert alert-#{key}"}= value
      = yield
    .footer
      .panel-footer
        A Simple Application for Beginners on RUBY ON RAILS



